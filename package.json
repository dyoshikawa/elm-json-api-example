{
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "clean": "rm -rf .cache dist elm-stuff",
    "server": "json-server --watch db.json"
  },
  "devDependencies": {
    "elm": "^0.19.0-bugfix6",
    "elm-hot": "^1.0.1",
    "json-server": "^0.14.2",
    "node-elm-compiler": "^5.0.1",
    "parcel-bundler": "^1.11.0",
    "typescript": "^3.3.3"
  }
}
